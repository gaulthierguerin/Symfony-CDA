{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="form-group">
            {{ form_row(form.ProductName) }}
    </div>
    <div class="form-group">
        {{ form_row(form.SupplierID) }}
    </div>

<div class="form-row">
    <div class="col-md-4">
        {{ form_row(form.CategoryID) }}
    </div>
    <div class="col-md-4">
        {{ form_row(form.QuantityPerUnit) }}
    </div>
    <div class="col-md-4">
        {{ form_row(form.UnitPrice) }}
    </div>
</div>
<div class="form-row">
    <div class="col-md-4">
        {{ form_row(form.UnitsInStock) }}
    </div>
    <div class="col-md-4">
        {{ form_row(form.UnitsOnOrder) }}
    </div>
    <div class="col-md-4 m-0">
        {{ form_row(form.ReorderLevel) }}
    </div>
</div>
{{ form_row(form.Discontinue) }}
<div class="row d-flex">
    <a href="{{ path('products_index') }}" class="btn btn-secondary mx-auto">Retour à la liste</a>
    <button class="btn btn-primary mx-auto">{{ button_label|default('Sauvegarder') }}</button>
</div>

{{ form_end(form) }}
